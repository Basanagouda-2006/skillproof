export default function AboutPage() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-16">
      <h1 className="text-3xl font-semibold font-display">About SkillProof</h1>
      <p className="text-slate-600 mt-4 leading-relaxed">
        SkillProof was built to close the gap between what candidates claim and what
        can actually be observed in their technical work. The product is built on one
        core principle: deterministic evidence first, AI second. AI is used only to
        explain evidence that has already been verified - never to generate it.
      </p>
    </div>
  );
}
